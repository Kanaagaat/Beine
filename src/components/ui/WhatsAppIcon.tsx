export function WhatsAppIcon({ className = 'h-5 w-5' }: { className?: string }) {
  return (
    <img
      src="/icons/icons8-whatsapp.svg"
      alt="WhatsApp"
      className={className}
    />
  );
}
